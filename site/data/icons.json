{
  "data": [
    {
      "icons": {
        "title": "SVG Icons",
        "describe": "The variation includes a couple of SVG icons that are included in the distributed CSS. The SVG icons are refined to use a single path to offset the size of the production files. The SVG icons are used as accents, like in button toggles, form validations and input feedback."
      }
    }
  ]
}
